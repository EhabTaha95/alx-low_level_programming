#include "main.h"

/**
 * print_alphabet - print alphabet in lower case
 *
 * Return: 0 exit code
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);

	_putchar('\n');
	}
	return (0);
}
